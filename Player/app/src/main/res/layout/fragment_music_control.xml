<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="32dp"
    android:orientation="horizontal"
    android:id="@+id/controlLayout"
    tools:context=".fragments.MusicControlFragment"
    android:background="@color/colorPrimaryDark"
>


    <ImageView
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:padding="5dp"
        android:layout_alignParentStart="true"
        android:id="@+id/playingMode"
        android:src="@drawable/ic_shuffle" />

    <ImageView
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:id="@+id/play_pause"
        android:src="@drawable/ic_play"
        android:padding="5dp"
        android:layout_alignParentEnd="true"/>

    <TextView
        android:singleLine="true"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:id="@+id/controlText"
        android:textAlignment="center"
        android:layout_centerInParent="true"
        android:textStyle="bold"
        android:textColor="@color/colorTextPrimary"
        android:textSize="@dimen/textItemSmallSize"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/playingMode"
        android:layout_toStartOf="@+id/play_pause"
        android:ellipsize="marquee"
        android:fadingEdge="horizontal"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"/>

    <TextView
        android:singleLine="true"
        android:layout_centerHorizontal="true"
        android:layout_width="match_parent"
        android:id="@+id/controlText2"
        android:textAlignment="center"
        android:layout_centerInParent="true"
        android:textStyle="bold"
        android:textColor="@color/colorTextPrimary"
        android:textSize="@dimen/textItemSmallSize"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/playingMode"
        android:layout_toStartOf="@+id/play_pause"
        android:ellipsize="marquee"
        android:fadingEdge="horizontal"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"/>

    <!--<RelativeLayout-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent"-->
        <!--android:id="@+id/click_zone_curr"></RelativeLayout>-->

</RelativeLayout>